import * as serverListeners from './server'
import * as userListeners from './user'
import * as siteListeners from './site'
import * as taskListeners from './task'

export default {
    serverListeners,
    userListeners,
    taskListeners,
    siteListeners,
}
